package com.scanner.utils.builder

import com.github.legend295.fingerprintscanner.R

class ThemeOptions {
    var buttonColor: Int = R.color.infraRed // default button color
    var buttonTextColor: Int = R.color.white // default button text color set to white
    var messageColor: Int = R.color.robinEggBlue // Message/Status text color
    var titleTextColor: Int = R.color.black // Title text color
    var contentTextColor: Int = R.color.black // Title text color
}